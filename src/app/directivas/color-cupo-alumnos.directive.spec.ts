import { ColorCupoAlumnosDirective } from './color-cupo-alumnos.directive';

describe('ColorCupoAlumnosDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorCupoAlumnosDirective();
    expect(directive).toBeTruthy();
  });
});
