import { ColorSituacionAlumnoDirective } from './color-situacion-alumno.directive';

describe('ColorSituacionAlumnoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorSituacionAlumnoDirective();
    expect(directive).toBeTruthy();
  });
});
