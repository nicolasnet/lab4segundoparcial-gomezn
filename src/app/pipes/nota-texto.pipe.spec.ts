import { NotaTextoPipe } from './nota-texto.pipe';

describe('NotaTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new NotaTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
