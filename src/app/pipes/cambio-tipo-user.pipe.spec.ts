import { CambioTipoUserPipe } from './cambio-tipo-user.pipe';

describe('CambioTipoUserPipe', () => {
  it('create an instance', () => {
    const pipe = new CambioTipoUserPipe();
    expect(pipe).toBeTruthy();
  });
});
